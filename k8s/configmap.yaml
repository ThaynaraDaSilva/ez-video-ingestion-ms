apiVersion: v1
kind: ConfigMap
metadata:
  name: ez-video-ingestion-configmap
data:
  AWS_REGION: "us-east-1"
  DYNAMODB_TABLE_NAME: "video_metadata"
  SQS_QUEUE_NAME: "video-processing-queue"
  S3_BUCKET_NAME: "ez-frame-video-storage"